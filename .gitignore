# Python
# backend/.venv was committed before this file existed - 11,517 files and most of
# the repo's 110 MB. Untracked now; the explicit path below is belt-and-braces so
# a `git add -f` habit can't quietly bring it back.
backend/.venv/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
.venv/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Testing
.pytest_cache/
.coverage
htmlcov/
*.cover
.hypothesis/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Jupyter
.ipynb_checkpoints/

# Models (keep committed for demo purposes, but exclude large temp files)
*.pkl

# Logs
*.log
logs/

# Environment variables
.env
.env.local

# Node modules (if we add frontend tooling)
node_modules/
package-lock.json
yarn.lock
